<%= link_to("Back to List", books_path, :class => 'back-link') %>

<div class="tasks edit">
  <h2>Edit Task</h2>

  <%= form_for(@book) do |f| %>
    <% # url: tasks_path(@task), method: 'patch' %>

    <%=render(partial: 'form', locals:{f:f})%>

      <td colspan="2"><%= f.submit "Save Book" %></td>

  <% end %>
</div>